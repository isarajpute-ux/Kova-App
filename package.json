{
  "name": "kova-app",
  "private": true,
  "type": "module",
  "description": "Kova - health, training, nutrition, sleep and work OS. Static PWA + Netlify Functions backend + Firebase.",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "check": "node scripts/check-html.mjs",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "firebase-admin": "^13.0.0"
  }
}
